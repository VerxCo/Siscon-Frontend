# Siscon Front

Frontend React + Vite para consumir o backend Siscon.

## Rodar

```powershell
npm install
npm run dev
```

Configure a API no `.env`:

```powershell
VITE_API_BASE_URL=http://127.0.0.1:8000
```

O backend precisa estar rodando na porta 8000.
